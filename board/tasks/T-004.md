# T-004 · The docs gate rejects the path a task card must name before it exists

- Owner: @Q1
- Reviewer: @human
- Milestone: S0
- Release condition it advances: S0 — "one project has run one task end to end under the
  framework". The first card a project wrote under v3.0.1 was rejected by the template that
  told it what to write.

## 1. Goal

A task card filled in from the template passes the docs gate, and the studio's own cards are
checked by the same rule a project's cards are.

## 2. Inputs

- `sunset-club` PR #3 — the card that was rejected
- `tools/gates/selfcheck.mjs` — the path-existence check
- `templates/board/tasks/TEMPLATE.md` — section 8, the line in question

## 3. Lane

```
tools/gates/selfcheck.mjs
CHANGELOG.md
evidence/T-004/**
```

## 4. Contracts

None. The template is unchanged; the gate stops contradicting it.

## 5. Definition of done

- [ ] A card naming `board/blockers/T-001.md` passes the docs gate in a project.
- [ ] The studio's path check covers the board directories the studio actually has, so a
      defect of this class fails here before it reaches a project.
- [ ] The studio's own selfcheck still passes, without exempting the paths it describes for
      projects and does not hold itself.

## 6. Acceptance command

```bash
node tools/gates/gates.test.mjs
node tools/gates/selfcheck.mjs
node tools/gates/spec-check.mjs
```

## 7. Evidence

`evidence/T-004/` — command.txt, output.txt, diff-stat.txt, env.txt.

## 8. Timeout and escalation

- Expected: one session
- If blocked: escalate to @human.
- Three failed attempts: stop and write `board/blockers/T-004.md`. There is no fourth.
